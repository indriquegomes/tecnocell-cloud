import { LoaderTecnoCell } from '@/components/LoaderTecnoCell'

// Tela de carregamento do painel (aparece ao abrir/logar, enquanto o dashboard
// busca os dados). A logo da TecnoCell quicando — a nossa cara enquanto carrega.
export default function Loading() {
  return <LoaderTecnoCell />
}
